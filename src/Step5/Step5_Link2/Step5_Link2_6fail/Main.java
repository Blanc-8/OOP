package Step5.Step5_Link2.Step5_Link2_6fail;

/*
Write a Java program to create a generic method that takes a list of any type and a predicate.
It returns an array list containing only elements that satisfy the predicate.

Напишите программу на Java для создания общего метода, который принимает список любого типа и предикат.
Он возвращает список массивов, содержащий только элементы, удовлетворяющие предикату.
 */
public class Main {
    // Новая штука predicate, не понимаю как тут использовать, оставлю на потом.
//    public static <T> List <T> predicate(List<T> list, Predicate<T> predicate) {
//        List<ArrayList<T>> listOfArrays = new ArrayList<>();
//        for (int i = 0; i < list.size(); i++) {
//            if (list.get(i) == predicate) {
//                listOfArrays.add(new ArrayList<T>= List.of(predicate.test(t)));
//            }
//
//        }
//    }
}
